console.log("확인");

// this 바인딩은 함수 호출 방식에 따라 동적으로 결정된다.
const foo = function () {
  console.dir(this);
};

// 동일한 함수도 다양한 방식으로 호출할 수 있다.

// 1. 일반 함수 호출
foo();
// 일반함수 호출은 어디에 위치하더라도
//  this는 전역객체인 window를 가리킨다.
// 이처럼 일반 함수로 호출된 모든 함수(중첩 함수 , 콜백 함수 포함)
// 내부의 this에는 전역객체가 바인딩 된다.
